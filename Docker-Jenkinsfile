pipeline {
   agent any
   
   options {
        timestamps()
        timeout(time: 1, unit: 'HOURS')
   }
   
   tools {
         maven 'Maven-363'
   }
   
   stages {
        stage('GIT-Checkout') {
             git 'https://github.com/vaheedgithubac/Dummy-myweb'
        }
   }
}
